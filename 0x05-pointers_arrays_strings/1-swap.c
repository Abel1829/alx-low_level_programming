#include "main.h"
/**
 * swap_int - A function that swaps the value of two integers.
 * @a: integer value that will be swaped.
 * @b: integer value that will be swaped.
 * Return: *a & *b.
 */
void swap_int(int *a, int *b)
{
	temp = *a;
	*a = *b;
	*b = temp;
}
