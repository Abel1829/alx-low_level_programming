#include "main.h"
/**
 * print_rev - A function that prints a string in reverse.
 *
 * @s: a pointer points to s.
 *
 * Return: void
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; i = i[s]; i--)
	{
		_putchat(i[s]);
	}
	_putchat('\n');
}
