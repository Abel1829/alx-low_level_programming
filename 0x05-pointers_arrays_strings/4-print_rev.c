#include "main.h"
/**
 * print_rev - A function that prints a string in reverse.
 *
 * @s: a pointer points to s.
 *
 * Return: void
 */
void print_rev(char *s)
{
	int i;

	for (s[i]; i = 0; i--)
	{
		_putchat(i[s]);
	}
	_putchat('\n');
}
