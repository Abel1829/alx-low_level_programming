#include "main.h"
/**
 * _puts - A function that prints a string.
 *
 * @str: a pointer that points to char.
 *
 * Return: string.
 */
void _puts(char *str)
{
	int i;

	for (i = 0; i[str]; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
