#include "main.h"
/**
 * _abs - A function that computes the absolute value of an integer.
 *
 * @i: An input as an integer.
 *
 * Return: Absoulute value.
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (0);
	}
	else
	{
		return (i * -1);
	}
}
